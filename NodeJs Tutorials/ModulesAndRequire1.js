var counter = function(arr) {

  return "There are " + arr.length + " elements in this array" ;

};

module.exports = counter; // This part of code says, i will make the function counter available for other modules calling this function
